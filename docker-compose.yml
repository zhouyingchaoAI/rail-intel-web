services:
  api:
    build:
      context: ./apps/api
    ports:
      - "8000:8000"
  web:
    build:
      context: ./apps/web
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://api:8000
    ports:
      - "3000:3000"
    depends_on:
      - api
